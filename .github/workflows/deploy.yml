name: DevOps Demo Pipline

on: 
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/Checkout@v4

      - name: Show Message
        run: echo "Hello Github Actions"